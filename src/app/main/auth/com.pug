button( mat-raised-button, (click)="authSrv.login('user_role_1')" )
  mat-icon lock_open
  | login as #[strong auth 1]

button( mat-raised-button, (click)="authSrv.login('user_role_2')" )
  mat-icon lock_open
  | login as #[strong auth 2]

button( mat-raised-button, (click)="authSrv.login('user_role_1')" )
  mat-icon lock_open
  | login as #[em both]